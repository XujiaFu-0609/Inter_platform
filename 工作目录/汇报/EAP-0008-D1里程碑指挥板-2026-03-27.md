# EAP-0008 D1 里程碑指挥板

日期：2026-03-27  
责任人：CTO  
对应任务：CLOA-76  
里程碑：D1 = 2026-03-28 18:00（Asia/Shanghai）

## 1. D1 目标

D1 目标不是交付完整实现，而是完成一期架构冻结、控制面边界冻结、平台态接口/枚举字典冻结，并形成 QA 可执行的 D1 验收口径。

## 2. 当前状态总览

| 任务 | 责任人 | 当前状态 | 对 D1 影响 | 截止时间 | 当前判断 |
| --- | --- | --- | --- | --- | --- |
| `CLOA-77` 后端 Go+Operator 重构设计 | Backend Engineer | 已完成 | 高 | 已完成 | 通过 |
| `CLOA-80` 前端 API 对齐与可视化草图 | Frontend Engineer | 已完成 | 中 | 已完成 | 通过 |
| `CLOA-81` D1/D3/D7 验收清单 | QA Lead | 已完成 | 高 | 已完成 | 通过 |
| `CLOA-78` Spark/Flink/Volcano 一期融合方案 | Big Data Architect | `todo` | 高 | 2026-03-28 12:00 初稿 | 风险中（尚无回执） |
| `CLOA-79` CRD/Controller 与运行基线 | Cloud Native Architect | `todo` | 高 | 2026-03-28 12:00 初稿 | 风险中（旧 run 已中断，待正式回稿） |
| `CLOA-82` 平台接口与枚举字典 D1 冻结 | Backend Engineer（联动 78/79） | `in_progress` | 高 | 2026-03-28 12:00 初稿 | 已收到 v0.1 初稿 |

## 2.1 最新回执快照

1. `CLOA-82` 已提交 v0.1 初稿，并补齐 `CTO 四问对照`，当前已覆盖 `platform/*` 核心接口、统一枚举、Grafana 深链字段、字段来源矩阵与联动边界。
2. `CLOA-82` 已向 `CLOA-78`、`CLOA-79` 发出字段对齐清单，明确了 `slaTier/queueUtilization` 与 `controlPlaneHealth/CRD->DTO` 的待回稿项。
3. `CLOA-79` 已出现系统接管旧 run 的记录，说明此前执行链路有残留，但当前仍未提交正式技术初稿。
4. `CLOA-78` 当前仍无任何正式技术回执，仅收到对齐请求，是 D1 最主要的不确定项。
5. 当前 D1 风险重心已从“平台 DTO 未成形”转移为“Spark/Flink/Volcano 队列口径与 CRD 状态边界尚未回稿”。

## 3. D1 必须冻结项

### 3.1 架构冻结

1. Go 后端 + PostgreSQL + Redis + Kafka 主路径冻结。
2. Spark/Flink on Kubernetes + Volcano 调度路径冻结。
3. 禁止引入 YARN 的约束冻结。

### 3.2 控制面冻结

1. `InterviewPlatform`
2. `BatchPipeline`
3. `StreamPipeline`
4. 上述 CRD 的 `.spec` / `.status` 字段边界与 Reconcile 范围

### 3.3 接口与字典冻结

1. `GET /api/v1/platform/overview`
2. `GET /api/v1/platform/runs`
3. `GET /api/v1/platform/runs/{runId}`
4. `GET /api/v1/platform/alerts`
5. 枚举字典：
   - `engineType`
   - `runStatus`
   - `alertSeverity`
   - `slaTier`
   - `queueName`
6. Grafana 深链字段：
   - `grafanaDashboardUid`
   - `grafanaPanelId`
   - `grafanaFrom`
   - `grafanaTo`
   - `grafanaVars`

## 4. 升级条件

### 4.1 标黄条件

满足任一条件即标黄：

1. `CLOA-78` / `CLOA-79` / `CLOA-82` 在 2026-03-28 12:00 前仍无初稿；
2. 平台态 DTO 与统一枚举字典仍未形成唯一版本；
3. QA 无法基于冻结稿执行 D1 验收。

### 4.2 标红条件

满足任一条件即标红：

1. 2026-03-28 18:00 前 CRD 边界仍未冻结；
2. 2026-03-28 18:00 前 Spark/Flink/Volcano 队列口径仍未冻结；
3. 2026-03-28 18:00 前 `platform/*` 接口 DTO 未冻结；
4. 出现新的 P0 架构冲突或关键约束被违反（如试图引入 YARN、非 Go 后端主线）。

## 5. CTO 行动预案

1. 2026-03-28 12:00 前：收集 `CLOA-78` / `CLOA-79` / `CLOA-82` 初稿并做交叉评审。
2. 若 12:00 未回稿：在父任务 `CLOA-76` 标黄并继续催办。
3. 若 18:00 仍未冻结：D1 结论仅能为 `Conditional Go` 或 `No-Go`，不得宣称完成。
4. D1 后立即更新 D3 联调关键路径与资源冲突列表。
